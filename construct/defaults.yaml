construct:
  version: -1
  git_repo: https://github.com/construct-org/construct_setup

  # The following defaults are set in map.jinja in the os_defaults map
  config: ''
  python_exe: ''
  install_dir: ''
